# infra — AWS CDK (Stage 4.4)
Planned stacks: static web hosting (S3+CloudFront), API (Fargate or Lambda),
Postgres (RDS), assets bucket, events pipeline (API GW → Firehose → S3).
You know CDK — init here (`cdk init app --language python`) when Stage 4 starts.
